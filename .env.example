# Environment variables for BCU USD Exchange Rate API
# Copy this file to .env and adjust values as needed

# Application environment
NODE_ENV=production

# Server port (container internal port - don't change unless you modify Dockerfile)
PORT=3000

# Host port for external access (change if 3000 is occupied)
HOST_PORT=3000

# Optional: Custom container name
CONTAINER_NAME=bcu-usd-api

# Optional: Memory limits (adjust based on your server capacity)
MEMORY_LIMIT=256M
MEMORY_RESERVATION=128M

# Optional: CPU limits
CPU_LIMIT=0.5
CPU_RESERVATION=0.25